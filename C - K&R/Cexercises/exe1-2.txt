/*Exercise 1-2. Experiment to find out what happens when prints's argument string contains
\c, where c is some character not listed above.*/

/*
//Hello.c

#include <stdio.h>

int main()
{
	printf("hello,\c world");
}
*/

//Apparently, if it is not a scape sequence like '\t', '\b', '\n', then it will pretty the letter and ignore '\', otherwise
//it will do whatever the given scape sequence is programmed to do.